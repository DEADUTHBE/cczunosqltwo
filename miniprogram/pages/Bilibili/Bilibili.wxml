<!--pages/HomePage.wxml-->
<view class="background">

    <view class="container">
        <view class="logo">
            <image class="img" src="../../images/HotSearch/Bilibili_iconSelected.png" mode="aspectFit"></image>
            <text class="title">Bilibili热搜</text>
        </view>
    </view>

    <view class="hotSearchBox" wx:for="{{hotData.bili}}">
        <view class="num">{{item.rank * 1 + 1}}</view>
        <view class="hotSearch">
            <view style="font-size:50rpx">{{item.title}}</view>
            <view style="color: rgb(153 153 153 / 1);">热度: {{item.score}}</view>
        </view>
    </view>

</view>
